# C Services

Low-level C implementations.

## Services

- **memory_allocator.c** - Custom memory allocators (arena, pool, stack)
- **cryptography.c** - Encryption, hashing, Base64 encoding

## Build

```bash
gcc -o app *.c
./app
```
